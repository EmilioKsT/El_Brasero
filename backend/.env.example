# Base de datos
MONGODB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/nombre_db

# Servidor
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# JWT (CRÍTICO: Genera un secreto seguro, nunca uses el ejemplo)
JWT_SECRET=tu_secreto_super_seguro_aqui_CAMBIALO

# CORS (En producción, lista de dominios separados por coma)
# Ejemplo: CORS_ORIGIN=https://tudominio.com,https://www.tudominio.com
CORS_ORIGIN=

# Logging
LOG_LEVEL=info
